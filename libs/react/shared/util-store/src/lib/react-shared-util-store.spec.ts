import { reactSharedUtilStore } from './react-shared-util-store';

describe('reactSharedUtilStore', () => {
  it('should work', () => {
    expect(reactSharedUtilStore()).toEqual('react-shared-util-store');
  });
});
